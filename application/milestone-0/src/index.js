import React from 'react';
import ReactDOM from 'react-dom/client';
import AboutUsPage from './pages/index.js';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AboutUsPage />
  </React.StrictMode>
);